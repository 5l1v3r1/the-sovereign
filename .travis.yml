language: php

before_script:
    - cp .env.travis .env
    - mysql -e 'create database thesovereign;'
    - composer self-update
    - composer install --no-interaction

script:
    - vendor/bin/phpunit
